

describe('Perf', function () {
	describe('Constructor', function () {


		// it('Should create an array of 10000 users', function (done) {
		// 	let orm = new Orm(opt, repositories)
		// 	orm.loadRepositories()
		// 	expect(() => {
		// 		orm.init().then(() => {
		// 			let i = 0
		// 			const users = Array.apply(null, Array(10000)).reduce(function (res) {
		// 				res.push({
		// 					email: 'test' + ++i + '@test.fr',
		// 					password: 'password',
		// 					firstName: 'firstName',
		// 					lastName: 'lastName'
		// 				})
		// 				return res
		// 			}, [])
		// 			User.create(users).then(users => {
		// 				//console.log('users created', users)
		// 				done()
		// 			}, err => {
		// 				console.log('err', err)
		// 			})
		// 		}).catch(err => {
		// 			console.log('Main catched', err)
		// 		})
		// 	}).not.to.throw(Error)
		// })

		// it('Should create an array of 10000 users slowly', function (done) {
		// 	let orm = new Orm(opt, repositories)
		// 	orm.loadRepositories()
		// 	expect(() => {
		// 		orm.init().then(() => {
		// 			let i = 0
		// 			const users = Array.apply(null, Array(10000)).reduce(function (res) {
		// 				res.push({
		// 					email: 'test' + ++i + '@test.fr',
		// 					password: 'password',
		// 					firstName: 'firstName',
		// 					lastName: 'lastName'
		// 				})
		// 				return res
		// 			}, [])
		// 			var d = function(){
		// 				i--
		// 				if (i == 0)
		// 				done()
		// 			}
		// 			users.map((elem)=>{
		// 				User.create(elem).then(d)
		// 			})

		// 			// User.create(users).then(users => {
		// 			// 	//console.log('users created', users)
		// 			// 	done()
		// 			// }, err => {
		// 			// 	console.log('err', err)
		// 			// })
		// 		}).catch(err => {
		// 			console.log('Main catched', err)
		// 		})
		// 	}).not.to.throw(Error)
		// })


        
    })
})